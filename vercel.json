{
    "version": 2,
    "builds": [{
        "src": "server.js",
        "use": "@vercel/node"
    }],
    "routes": [{
        "src": "/(._)",
        "dest": "server.js"
    }],
    "build": {
        "env": {
            "NPM_FLAGS": "--legacy-peer-deps"
        }
    }
}